# DSH 文档导入插件（dsh-doc-import）

一个 DeepSeek Harness Web 插件，让用户把文档（txt/md/pdf/docx/csv）拖入对话窗口，由插件
自动解析、存储、必要时 OCR，并以"文件卡片"形式进入对话。

本文件是**词汇表**：只定义本项目特有的概念，不记录实现细节、决策或进度。

## Language

**文档导入 (Document Import)**:
把文档文件拖入/粘贴进对话窗口、由插件接管后续解析与入模的完整动作。
_Avoid_: 上传文件、附件上传（后者指 shell 的图片管线）

**解析 (Extraction)**:
把文档字节转为模型可读文本的动作，按格式分派解析器（pdf 文本层、docx→markdown 等）。
_Avoid_: 读取、加载

**OCR 回退 (OCR Fallback)**:
当 PDF 页面文本层缺失或过少（扫描件）时，把该页转图交给视觉模型识别文本的动作；按页进行，
与文本层页面混合组装。
_Avoid_: 图像识别、识图（泛指视觉能力时用）

**引用行 (Reference Line)**:
发送时进入消息正文的一行紧凑标记 `[document 名称, 类型, 页数/字符数, id: sha256]`，
其后可选跟一句给模型读的引导语（含 shell 可读路径）。它是模型获知文档存在的唯一线索。
_Avoid_: 附件说明、header（口语可，但正式措辞用"引用行"）

**文件卡片 (File Chip)**:
聊天转录区里把引用行**渲染**成的可点击卡片（图标 + 文件名 + 元信息）；点击可看全文预览、
打开原始文件。仅存在于渲染层，消息文本本身不被改写。
_Avoid_: 附件气泡、缩略图

**草稿 (Draft)**:
已导入但尚未随消息发出的文档状态（含解析/OCR 进度）；发送成功后从草稿区清除。
草稿是**全局**的，跨对话与页面刷新保留。
_Avoid_: 待发送附件、暂存文件

**全文回读 (Full-Text Readback)**:
模型通过 `read_document` 工具按文档 id 重新获取解析全文（支持 offset 分页）的动作，
不占用消息正文额度。
_Avoid_: 重读、再解析

## 决策与进度的位置

- 架构取舍 → [`docs/adr/`](./docs/adr/)（0001 文本内联 → 0002 纯文件引用 → 0003 草稿全局化）
- 当前状态 / 下一步 / 踩坑 → [`docs/PROGRESS.md`](./docs/PROGRESS.md)
- 功能清单与后续方向 → [`docs/roadmap.md`](./docs/roadmap.md)
- 协作规则与工程约束 → [`AGENTS.md`](./AGENTS.md)
